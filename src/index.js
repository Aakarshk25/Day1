import React from "react";
import  ReactDOM  from "react-dom/client";// clinet we are using to convert it into version 18 react 
import App from './App';
import './index.css'

// ReactDOM.render(<App />, document.getElementById("root"));
const root = ReactDOM.createRoot(document.getElementById('root'));

root.render(<App />)
